# 进度快照 122 — 远航版后端部署并修正为 1.0.17 版本链

> 日期: 2026-05-28

---

## 已完成内容

### 1. 已确认首次启动报错的真实原因

- 远航版启动器默认更新地址已指向：
  - 游戏更新：`3014`
  - 启动器安装包更新：`3015`
- 报错根因不是前端逻辑错误，而是远航版双后端当时**尚未部署**
- 初次巡检结果：
  - `ee2x-update-yh.service` 不存在
  - `ee2x-launcher-update-yh.service` 不存在
  - `/opt/ee2/ee2x-new-update/` 目录不存在

### 2. 已完成远航版双后端正式部署

- 已部署：
  - `更新器/update_backend_yh/` → `http://115.231.35.105:3014`
  - `更新器/launcher_update_backend_yh/` → `http://115.231.35.105:3015`
- 当前健康检查正常：
  - `GET /api/update/v1/health` 返回 `ok=true`
  - `GET /api/launcher-update/v1/health` 返回 `ok=true`

### 3. 已纠正远航版数据源误导入问题

- 初次部署时曾将魔改版 `3010` 全量历史导入到远航版 `3014`
- 用户已明确指出：
  - **魔改版 3010 数据与远航版完全不同**
- 随后已在服务器上修正为仅保留远航版自己的历史链：
  - `1.0.11`
  - `1.0.13`
  - `1.0.14`
  - `1.0.15`
  - `1.0.17`
- 已确认当前远航版 `stable`：
  - `currentVersion = 1.0.17`
  - `historyTotalCount = 5`
  - latest 已正确指向 `1.0.17`

### 4. 已完成远航版安装包更新发布

- 已将远航版启动器安装包 `1.0.31` 发布到 `3015`
- 当前验证结果：
  - `GET /api/launcher-update/v1/history?limit=0` 返回 `1.0.31`
  - `GET /api/launcher-update/v1/check/windows/x86_64/1.0.31` 返回 `204`
  - `GET /api/launcher-update/v1/check/windows/x86_64/1.0.30` 返回可升级信息

### 5. 用户可用性结论

- 远航版首次启动所依赖的 `3014` 版本检查链已恢复正常
- 远航版安装包自更新所依赖的 `3015` 也已可用
- 当前可分发给用户使用的版本：
  - `EE2X远航版启动器 1.0.31`

---

## 当前修改到的模块

- `更新器/update_backend_yh/README.md`
- `进度快照/`
- 远端服务器：
  - `/opt/ee2/ee2x-new-update/game-backend`
  - `/opt/ee2/ee2x-new-update/launcher-backend`

---

## 待办清单

- 如需继续迭代远航版游戏内容：
  - 后续新版本应发布到远航版 `3014`，不要再混入魔改版数据链
- 如需继续迭代远航版启动器安装包：
  - 后续安装包发布到远航版 `3015`
- 如需验证用户真实首次启动体验：
  - 用全新机器或清空本地远航版配置目录后做一次完整首启验证

---

## 下一步建议

- 直接用 `EE2X远航版启动器_1.0.31_x64-setup.exe` 做一次用户视角安装测试
- 重点确认：
  - 首次启动不再弹“首次启动检查更新失败”
  - 首启更新完成后可进入登录界面
