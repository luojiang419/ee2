# 进度快照 055 — UP1.6 冻结保护落地

> 日期: 2026-05-21

---

## 已完成内容

### 1. 官方更新链已默认冻结 UP1.6 体系

- `更新器/src/ee2x_update_suite/shared/constants.py`
  - 新增：
    - `UP_FROZEN_PATTERNS`
    - `UP_TEXT_SYNC_GROUP`
- 冻结区默认禁止被官方更新链覆盖或删除：
  - `EE2.exe`
  - `EE2X.exe`
  - `UP15.dll`
  - `UP15_GameHelper.dll`
  - `UnofficialVersionConfig.txt`
  - `Unofficial Patch Files/**`

### 2. 本地打包阶段已阻断 UP 冻结区与文本联动不完整

- `更新器/src/ee2x_update_suite/shared/package_rules.py`
- 新规则：
  - 选择命中冻结区 → `up-frozen-selection` 错误
  - 只选中 `dbtext_enums / dbtext_cheats / TextsSource.txt` 任意子集 → `up-text-sync-incomplete` 错误
- 该规则在 `prepare-dual / export-bundle` 阶段直接阻断，不再仅告警

### 3. 服务端发布入口已做二次拦截

- `更新器/update_backend_mg/app/service.py`
- 新增 `validate_release_payload(...)`
- 行为：
  - 即使绕过本地打包器手工造 bundle
  - 只要 manifest 的 `files[] / deleteList[]` 命中 UP 冻结区
  - 或命中 UP 文本联动组但不完整
  - 服务端直接 `400`

### 4. patcher_v2 已做最终兜底保护

- `更新器/src/ee2x_update_suite/patcher_v2/core.py`
- `更新器/src/ee2x_update_suite/shared/models.py`
- 新行为：
  - 运行前日志记录：
    - 目标 `game_root`
    - `scope`
    - manifest 命中的 UP 冻结区
  - 应用时：
    - 冻结区覆盖路径强制跳过
    - 冻结区删除路径强制跳过
    - 写日志标记：
      - `UP冻结跳过`
      - `UP冻结跳过删除`
  - 结果 JSON 新增：
    - `skippedProtectedPaths`

### 5. 发布端文案已同步

- `更新器/flutter_publish_tool/lib/main.dart`
- `更新器/update_backend_mg/app/publish/assets/app.js`
- 已明确显示：
  - 未勾选文件不会被自动删除
  - UP1.6 保护已启用，官方更新不会覆盖其冻结区文件

### 6. 自动回归已通过

- `python3 -m compileall` 通过：
  - `shared/constants.py`
  - `shared/package_rules.py`
  - `patcher_v2/`
  - `update_backend_mg/app/service.py`
  - `publisher/smoke_test.py`
- `publisher.smoke_test` 已通过，并新增覆盖：
  - 本地选择 `UnofficialVersionConfig.txt` 被阻断
  - 本地仅选 `dbtext_enums.utf8` 被阻断
  - 手工构造冻结区 release，服务端发布返回 `400`
  - patcher 运行时混入冻结区文件时：
    - 正常补丁文件仍可应用
    - `UnofficialVersionConfig.txt` 不会被覆盖
    - `Unofficial Patch Files/EnabledUP15Units/EE2X.exe` 不会被删除
    - `skippedProtectedPaths` 会记录这些路径

---

## 当前修改到的模块

- `更新器/src/ee2x_update_suite/shared/constants.py`
- `更新器/src/ee2x_update_suite/shared/package_rules.py`
- `更新器/update_backend_mg/app/service.py`
- `更新器/src/ee2x_update_suite/shared/models.py`
- `更新器/src/ee2x_update_suite/patcher_v2/core.py`
- `更新器/src/ee2x_update_suite/publisher/smoke_test.py`
- `更新器/flutter_publish_tool/lib/main.dart`
- `更新器/update_backend_mg/app/publish/assets/app.js`

---

## 待办清单

- [ ] 将本轮 UP1.6 冻结保护代码部署到 3010
- [ ] 用真实带 UP1.6 的游戏目录做一次官方更新真机验收
- [ ] 验证更新前后：
  - `UnofficialVersionConfig.txt` 未变化
  - `UP15.dll / UP15_GameHelper.dll` 未变化
  - `Unofficial Patch Files/**` 未变化
  - 游戏仍能正常启动

---

## 下一步

- 先提交并推送本轮冻结保护代码
- 然后部署 3010
- 再对真实带 UP1.6 的目录执行一次官方更新验收
