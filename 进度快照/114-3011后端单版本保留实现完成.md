# 进度快照 114 — 3011 后端单版本保留实现完成

> 日期: 2026-05-27

---

## 已完成内容

### 1. 3011 发布流程已改为单版本保留

- 修改位置：
  - `更新器/launcher_update_backend/app/service.py`
- 当前行为：
  - 发布新版本成功后，自动清理所有旧版本目录与数据库记录
  - `3011` 只保留当前最新一个启动器安装包版本

### 2. 清理失败回滚已补齐

- 发布时会先为旧版本目录做临时快照
- 若旧版本删除失败：
  - 数据库事务回滚
  - 旧版本目录恢复
  - 新版本目录清理
  - `latest.json` 回滚到数据库当前 latest

### 3. `/history` 语义已收口

- 因数据库中只保留当前 latest
- 所以：
  - `historyTotalCount` 只会是 `0` 或 `1`
  - `history` 只会返回当前保留版本
- 旧版本下载链接在下一次成功发布后将自然变为 `404`

### 4. 文档与备份已更新

- 文档：
  - `更新器/launcher_update_backend/README.md`
- 备份：
  - `backup/EE2X_db-141-3011后端单版本保留-pre.zip`
  - `backup/INDEX.md` 已登记

### 5. 已补本地回归测试

- 新增：
  - `更新器/launcher_update_backend/tests/test_single_version_retention.py`
- 覆盖：
  - 首次发布
  - 第二次发布替换旧版
  - 多次发布只留最新一版
  - 清理失败回滚

---

## 验证结果

- `python3 -m py_compile` 通过
- `python3 -m unittest discover -s 更新器/launcher_update_backend/tests -v` 通过
- 共 4 条测试，全部 `OK`

---

## 当前结论

- **3011 后端“上传新版本后自动删除旧版本，只保留 latest”已在代码层实现完成**
- 下一步只需：
  - 部署到线上 `115.231.35.105:3011`
  - 用下一次真实发布验证线上旧版本是否被自动清空

---

## 待办清单

- 部署后端到线上 3011
- 下一次真实发布后回读：
  - `/api/launcher-update/v1/history`
  - `/launcher-updates/latest.json`
  - 旧版本下载链接是否返回 `404`
