# 进度快照 084 — 新版启动器首页 960 分辨率可读性放大

> 日期: 2026-05-25

---

## 已完成内容

### 1. 首页新增专用放大模式

- 在 `EE2X魔改版启动器/src/App.tsx` 中为首页挂载 `home-mode` 样式标记
- 仅当 `activePage === "home"` 时启用，不影响更新页、设置页、在线玩家页的普通布局

### 2. 首页关键元素按 960x540 可读性重新放大

- 在 `EE2X魔改版启动器/src/styles.css` 中新增首页专用样式覆盖
- 放大对象包括：
  - 顶栏品牌区
  - 网络状态 / 版本 / 用户区
  - 登录注册按钮
  - 左侧在线玩家面板
  - 右侧联机状态块
  - 一键更新 / 游戏设置 / 开始游戏按钮
  - 首次选路径页的标题与主按钮
  - 底部状态条

### 3. 保持 960x540 窗口内完整显示

- 没有直接提高整页 `uiScale`
- 采用“首页专用局部放大 + 重新分配留白”的方式提升可读性
- 重点增大文字、按钮、状态块尺寸，同时扩大首页左右区块占比，避免简单整体缩放导致溢出

### 4. 已完成编译验证

- 在 `EE2X魔改版启动器/` 执行 `npm run build`
- `tsc` 与 `vite build` 均通过

---

## 当前修改模块

- `EE2X魔改版启动器/src/App.tsx`
- `EE2X魔改版启动器/src/styles.css`

---

## 待办清单

- 实机查看 960x540 首页，确认顶部信息条、左侧玩家表和右侧大按钮是否还有局部拥挤
- 如需继续加强可读性，可进一步细调：
  - 顶栏信息块宽度
  - 在线玩家表每行高度
  - 开始游戏按钮高度与文案字重
  - 首次选路径页标题的断行策略

---

## 下一步建议

- 直接启动新版启动器，在 960x540 下检查首页视觉效果
- 如果用户仍觉得偏小，下一轮优先做“顶部状态条”和“左侧玩家榜”的第二次放大微调
