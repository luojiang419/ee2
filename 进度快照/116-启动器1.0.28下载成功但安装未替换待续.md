# 进度快照 116 — 启动器 1.0.28 下载成功但安装未替换待续

> 日期: 2026-05-27

---

## 已完成内容

### 1. 服务器安装包后端当前状态

- `3011` 当前 latest 版本为 `1.0.28`
- 接口可正常返回：
  - `history`
  - `check/windows/x86_64/1.0.26`
  - `latest.json`

### 2. 真实客户端验证结论

已对隔离安装目录 `G:\ee2x-validate-126\install` 中的 `1.0.26` 客户端做真实验证，结果如下：

- 客户端会主动请求：
  - `GET /api/launcher-update/v1/check/windows/x86_64/1.0.26`
- 服务端会返回：
  - `200 OK`
- 客户端会自动下载：
  - `GET /launcher-updates/releases/1.0.28/updater.nsis.zip`
- `updaterDownloadCount` 已增至 `13`
- 旧版 `1.0.26` 进程会自动退出

### 3. 当前未闭环点

- 安装目录：
  - `G:\ee2x-validate-126\install\ee2x_mod_launcher.exe`
- 当前文件版本仍为：
  - `1.0.26`

结论：

- **服务器推送可收到**
- **updater 包可自动下载**
- **旧进程会自动退出**
- **安装替换落盘仍未成功**

---

## 当前机器安装状态

- 正式安装目录：
  - `C:\Program Files\EE2X魔改版启动器\`
  - 版本：`1.0.23`
- 隔离验证安装目录：
  - `G:\ee2x-validate-126\install\`
  - 版本：`1.0.26`

说明：

- 当前机器上没有真实正式安装的 `1.0.26`
- 本轮关于 `1.0.26 -> 1.0.28` 的所有自动升级验证，都基于隔离安装目录完成

---

## 待办清单

- 查清为什么 `plugin-updater` 已能完成：
  - 检查
  - 下载
  - 旧进程退出
- 但未能完成：
  - 安装目录版本替换

- 优先排查方向：
  - 自定义安装目录是否导致 updater 无法正确回填安装路径
  - `currentUser` + NSIS silent 模式下是否需要显式传递安装目录
  - Tauri updater v1Compatible 产物在当前场景下是否仍缺少额外元数据或安装参数

---

## 下一步建议

- 下一轮直接从“1.0.28 已下载、旧进程已退出、安装目录未替换”继续查
- 不再继续堆版本号，先把安装落盘链打通再发新版本
