# 进度快照 065 — 移除更新器开发代码并保留占位按钮

> 日期: 2026-05-22

---

## 已完成内容

### 1. 已移除整套更新器开发代码

- 已删除目录:
  - `更新器/`
- 覆盖范围:
  - Python 更新器源码
  - Flutter 发布端
  - HTTP 更新后端
  - patcher/build/spec/bridge/publisher/v2 等全部开发代码

### 2. 已清理本地更新器测试垃圾与缓存

- 已删除:
  - `build/`
  - `dist/`
  - `ee2x-patcher.spec`
  - `/tmp/ee2x-tools` 相关临时工具
- 已清理启动器目录中的旧更新残留:
  - `update/runtime/`
  - `update/ee2x-patcher.exe`
  - `update/ee2x-patcher-cli.exe`

### 3. 启动器已关闭一键更新实际功能

- 修改文件:
  - `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/main.js`
  - `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/renderer/renderer.js`
  - `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/package.json`
- 行为变化:
  - 仍保留 `一键更新` 按钮
  - 按钮文本改为 `一键更新（开发预留）`
  - 点击后仅提示“更新功能暂未开放，按钮仅保留为后续开发入口”
  - 不再拉起任何更新器/patcher/外部更新流程

### 4. 已移除本地 v2 更新执行器痕迹

- 已删除:
  - `Core/resources/app/updateV2.js`
- 已移除:
  - `velopack` 依赖
  - 启动器内的自动迁移/恢复/运行时校验接管逻辑

---

## 当前修改到的模块

- 启动器主进程更新入口
- 启动器渲染层更新按钮行为
- 本地更新器开发目录与缓存清理

---

## 待办清单

- [ ] 如后续重新开发更新功能，再从按钮占位入口重新接回新方案
- [ ] 如需同步到远端仓库，先解决当前机器 Git 凭据问题

---

## 下一步

- 当前已回到“无更新器、仅保留按钮占位”的状态
- 后续如果需要重新做更新体系，应新开目录、从零设计，不再恢复旧代码
