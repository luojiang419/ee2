# 进度快照 073 — 新版启动器编译完成与更新链验证通过

> 日期: 2026-05-25

---

## 已完成内容

### 1. 新版启动器已完成 Windows 打包

- 项目：`EE2X魔改版启动器/`
- 已确认 `npm exec tauri build` 成功
- 产物已生成：
  - `EE2X魔改版启动器/src-tauri/target/release/ee2x_mod_launcher.exe`
  - `EE2X魔改版启动器/src-tauri/target/release/bundle/nsis/EE2X魔改版启动器_0.1.0_x64-setup.exe`

### 2. 启动器 Rust 内核已补测试友好能力

- `src-tauri/src/lib.rs`
  - 新增启动器安装目录 override：`EE2X_LAUNCHER_INSTALL_DIR_OVERRIDE`
  - 新增应用数据目录 override：`EE2X_LAUNCHER_APPDATA_DIR_OVERRIDE`
  - 内部文件路径、配置、release-state、更新 staging 等 helper 已改为 runtime 泛型
  - `check_updates` / `run_update` 已拆出内部实现，便于后续继续做 headless / smoke 接线

### 3. 更新链 smoke 已修通并验证通过

- 已修复 `更新器/src/ee2x_update_suite/publisher/smoke_test.py` 中的过期断言与跨平台问题：
  - 移除对 `dbtext_enums.utf8` 旧阻断规则的错误预期
  - 补充缺失的 `hashlib` 导入
  - headless patcher 调用改为复用当前 `sys.executable`
  - Windows smoke launcher stub 改为 `.cmd`，不再伪装文本 `.exe`
  - 重启标记检查改为 best-effort，不再误伤主链
  - 结果 JSON 输出改为 UTF-8 bytes 写出
- 已修复 `更新器/src/ee2x_update_suite/patcher_v2/core.py` 中 PowerShell / tasklist 输出的编码兼容问题

### 4. 更新链验证结果

- smoke 已完整通过，覆盖：
  - 3 个版本连续发布：`9.9.101 → 9.9.102 → 9.9.103`
  - launcher + game 双 scope 下载与应用
  - `latest` / `history` 接口校验
  - release-state 写回
  - 删除旧版本后的 latest 回退
  - bundle 发布 / 删除链路
- 关键结果：
  - `executedScopes = [launcher, game]`
  - `restartedLauncher = true`
  - `releaseState.version = 9.9.102`
  - 删除最后版本后 `historyTotalCount = 0`

---

## 当前修改到的模块

- `EE2X魔改版启动器/.gitignore`
- `EE2X魔改版启动器/src-tauri/src/lib.rs`
- `EE2X魔改版启动器/src-tauri/Cargo.toml`
- `更新器/src/ee2x_update_suite/publisher/smoke_test.py`
- `更新器/src/ee2x_update_suite/patcher_v2/core.py`

---

## 待办清单

- [ ] 真实桌面端安装后，手动验证首页交互与背景导入体验
- [ ] 将新启动器接入真实发布版本号与正式资源目录
- [ ] 如需长期保留，给新版启动器补独立 headless smoke 入口
- [ ] 收敛新版启动器源码首次入库清单，只提交源码与必要资源

---

## 下一步

- 提交本轮源码与 smoke 修复
- 推送到 `main`
- 后续如继续推进新版启动器，可直接进入：
  - 安装包实机安装验证
  - 真实服务端更新联调
  - UI/设置页细节收敛
