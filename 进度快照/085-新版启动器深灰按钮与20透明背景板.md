# 进度快照 085 — 新版启动器深灰按钮与 20% 透明背景板

> 日期: 2026-05-25

---

## 已完成内容

### 1. 所有按钮统一改为深灰色配色

- 在 `EE2X魔改版启动器/src/styles.css` 顶部新增按钮主题变量
- 统一覆盖以下按钮样式：
  - `mini-action`
  - `mini-secondary`
  - `primary-glow`
  - `stack-action`
  - `text-button`
  - `path-button`
  - `picker-button`
  - `start-button`
  - `user-pill`
- 悬停状态同步调整为更亮一档的深灰色，而非原先蓝色系

### 2. 背景板透明度统一调整为 20%

- 新增 `--panel-bg: rgba(9, 14, 21, 0.2)`
- 统一应用到以下背景板：
  - `glass`
  - `glass-lite`
  - `modal`
  - `summary-block`
  - `notes-panel`
  - `profile-note`

### 3. 保持现有布局和首页放大逻辑不变

- 本次只调整配色与透明度
- 不改首页放大尺寸、不改按钮布局、不改窗口尺寸

### 4. 已完成构建验证

- 在 `EE2X魔改版启动器/` 执行 `npm run build`
- `tsc` 与 `vite build` 均通过

---

## 当前修改模块

- `EE2X魔改版启动器/src/styles.css`

---

## 待办清单

- 实机查看深灰按钮在背景图/背景视频下的识别度
- 观察 20% 透明背景板是否已经达到想要的通透感
- 如果还需要继续压暗界面，可以下一轮再统一降低按钮高光和边框亮度

---

## 下一步建议

- 直接测试首页、设置页、更新页三处按钮观感
- 如果用户觉得深灰还不够稳重，下一轮优先继续下调 hover 亮度与阴影强度
