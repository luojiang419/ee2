# 进度快照 022 — 更新器服务器接入与烟雾测试完成

> 日期: 2026-05-20

---

## 已完成内容

### 服务器真实更新服务已定位

文档中的旧路径 `/root/ee2x/ee2x_up-mg` 不存在，实际运行中的更新服务为：

- 目录：`/opt/ee2x_up-xg`
- 进程：`pm2` 下的 `ee2x-xg`
- 端口：`1234`

旧服务 `/opt/ee2x/ee2x_up`（1888） 保持不动。

### 远端静态更新目录已接入

已修改远端 `/opt/ee2x_up-xg/main.py`，新增：

- `STATIC_UPDATES_DIR = /opt/ee2x_up-xg/updates`
- `app.mount("/updates", StaticFiles(...), name="updates")`

并已重启 `pm2` 进程 `ee2x-xg` 验证在线。

远端更新目录已创建：

- `/opt/ee2x_up-xg/updates/stable/releases`
- `/opt/ee2x_up-xg/updates/codex-smoke/releases`

### 本地发布配置已就位

已创建本地忽略文件：

- `更新器/config/publish.local.json`

用于后续包工具直接推送正式版本。

### 发布链路烟雾测试通过

使用测试频道 `codex-smoke` 完成：

1. 本地生成标准 ZIP
2. 通过 `publisher/service.py` SFTP 上传
3. 远端生成并替换 `latest.json`
4. 公网验证：
   - `latest.json` 可访问
   - `release-manifest.json` 可访问
   - ZIP 可下载

### 客户端读取真实远端已验证

本地新更新器已对真实远端 `http://115.231.35.105:1234/updates/codex-smoke/latest.json` 完成一次完整应用，成功写入：

- `release-state.json`
- 目标文件 `smoke/hello.txt`

---

## 修改的文件

| 文件/目录 | 修改内容 |
|:-----|:---------|
| `更新器/config/publish.local.json` | 本地真实发布配置（已被 gitignore 忽略） |
| `更新器/docs/服务器部署说明.md` | 记录真实远端结构与接入结果 |
| 远端 `/opt/ee2x_up-xg/main.py` | 新增 `/updates` 静态挂载 |

---

## 远端备份

| 备份 | 路径 |
|:-----|:-----|
| 远端服务源码备份 | `/opt/ee2x_up-xg/main.py.backup_codex_20260520_051543` |

---

## 已验证

- `http://115.231.35.105:1234/updates/health.txt`
- `http://115.231.35.105:1234/updates/codex-smoke/latest.json`
- `http://115.231.35.105:1234/updates/codex-smoke/releases/codex-smoke-20260520/release-manifest.json`
- 测试 ZIP 可下载
- 本地新更新器可从真实远端完成应用

---

## 待办 / 遗留问题

- [ ] 在 Windows 环境打包正式 `ee2x-up.exe`
- [ ] 把新 `ee2x-up.exe` 放入实际启动器 `update/` 目录
- [ ] 确定正式版本号、正式更新说明、正式入包文件范围
- [ ] 发布第一个 `stable` 正式版本
- [ ] 用真实游戏目录验证正式一键更新

---

## 下一步

- 用当前包工具从正式 `Empire Earth II/` 文件中选择要发布的文件
- 生成正式版本 ZIP 和 manifest
- 发布到 `stable`
- 进行真实玩家环境验证
