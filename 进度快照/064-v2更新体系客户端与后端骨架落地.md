# 进度快照 064 — v2 更新体系客户端与后端骨架落地

> 日期: 2026-05-22

---

## 已完成内容

### 1. 启动器已接入新的 v2 更新执行器

- 新增模块:
  - `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/updateV2.js`
- 已落地能力:
  - `%LocalAppData%/EE2X/` 下的状态/事务/备份/补丁目录
  - 首次迁移导入与 legacy 清理
  - `runtime` 全量包下载、SHA-256 校验、备份、覆盖、失败回滚
  - `content` 通过 `butler.exe` 的 delta/full 选择与应用
  - 启动游戏前对 `EE2X.exe / UP15.dll / UP15_GameHelper.dll / UnofficialVersionConfig.txt / EnabledUP15Units/EE2X.exe` 的硬门禁检查

### 2. `main.js` 已从旧 patcher 链路切到 v2 路由

- 修改文件:
  - `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/main.js`
- 行为变化:
  - 启动器启动时执行 v2 迁移与 pending transaction 恢复
  - `launcher:runUpdater` 改为直接走 `updateV2.runUpdateRequest()`
  - `game:start` 改为先做运行时健康检查，再允许启动游戏
  - 旧 `update2:manifest` / `update2:download` 已明确拒绝，防止继续走旧 ZIP 覆盖链路

### 3. 启动器包元数据已切到 2.0.0 + Velopack

- 修改文件:
  - `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/package.json`
  - `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/package-lock.json`
- 已完成:
  - 版本号改为 `2.0.0`
  - 增加 `velopack` 依赖
  - 增加 `build:portable-dir` 脚本

### 4. Python 发布工具已新增 v2 bundle/HTTP 能力

- 新增模块:
  - `更新器/src/ee2x_update_suite/v2/__init__.py`
  - `更新器/src/ee2x_update_suite/v2/bundle.py`
  - `更新器/src/ee2x_update_suite/v2/http_backend.py`
- 修改:
  - `更新器/src/ee2x_update_suite/bridge/__main__.py`
  - `更新器/config/publish.example.json`
- 已落地命令:
  - `remote-info`
  - `release-history`
  - `prepare-v2-bundle`
  - `export-v2-bundle`
  - `publish-v2-bundle`
  - `promote-release`
  - `rollback-release`

### 5. 后端已切到 v2 数据模型和路由

- 修改文件:
  - `更新器/update_backend_mg/app/db.py`
  - `更新器/update_backend_mg/app/service.py`
  - `更新器/update_backend_mg/app/main.py`
- 已落地:
  - `launcher/runtime/content/cleanup` 四类资产的 v2 存储模型
  - `GET /api/update/v2/channels/{channel}/manifest`
  - `GET /api/update/v2/channels/{channel}/history`
  - `POST /api/update/v2/releases/publish-bundle`
  - `POST /api/update/v2/channels/{channel}/promote/{releaseId}`
  - `POST /api/update/v2/channels/{channel}/rollback/{releaseId}`
  - `/updates/v2/launcher/...`
  - `/updates/v2/runtime/...`
  - `/updates/v2/content/...`
  - `deltaFrom` 必须指向当前 latest 的服务端校验

### 6. 已完成静态校验

- Python:
  - `python3 -m py_compile` 已通过
- JavaScript:
  - `node.exe --check main.js`
  - `node.exe --check updateV2.js`
  - 均已通过

---

## 当前修改到的模块

- 启动器主进程更新链
- v2 本地更新执行器
- v2 Python bundle 构建
- v2 HTTP 发布客户端
- v2 后端存储与发布路由

---

## 待办清单

- [ ] 用真实 `vpk` 跑通 launcher feed 产物生成并验证 `Velopack` 更新闭环
- [ ] 用真实 `butler.exe` 跑通 content full/delta 产物生成并验证客户端 apply 命令参数
- [ ] 补一轮后端端到端发布/推广/回滚集成测试
- [ ] 更新 `更新器/README.md`，删掉旧 dual bundle / patcher_v2 / Flutter 发布端说明
- [ ] 视新链路稳定度，物理删除仓库中的 `patcher_v2` 和旧发布端代码

---

## 下一步

- 优先用真实工具链验证:
  - `prepare-v2-bundle`
  - `publish-v2-bundle`
  - 启动器 `launcher:runUpdater`
- 确认 `Velopack` 与 `butler` 的实际命令参数无误后，再执行旧模块物理清理
