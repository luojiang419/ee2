# 进度快照 108 — 启动器 1.0.24 已发布到 3011 待手动升级验证

> 日期: 2026-05-27

---

## 已完成内容

### 1. 1.0.24 正式安装包已构建

- 正式版本号已固定到 `1.0.24`
- 已产出真实安装包：
  - `EE2X魔改版启动器_1.0.24_x64-setup.exe`
- 已产出真实 updater 包：
  - `EE2X魔改版启动器_1.0.24_x64-setup.nsis.zip`
  - `EE2X魔改版启动器_1.0.24_x64-setup.nsis.zip.sig`

### 2. 签名构建链已修正

- `build-release.mjs` 已改为读取私钥内容并注入 `TAURI_SIGNING_PRIVATE_KEY`
- 不再依赖 `TAURI_SIGNING_PRIVATE_KEY_PATH + 空密码` 的错误方式
- `tauri.conf.json` 的 `createUpdaterArtifacts` 已改为 `v1Compatible`
  - 目的：稳定生成 `.nsis.zip + .sig`
  - 与当前 `3011` 后端和发布脚本保持一致

### 3. 1.0.24 已发布到 3011 后端

- 当前 latest:
  - `http://115.231.35.105:3011/api/launcher-update/v1/history?limit=0`
- 当前检查接口：
  - `http://115.231.35.105:3011/api/launcher-update/v1/check/windows/x86_64/1.0.23`
- 当前 latest.json：
  - `http://115.231.35.105:3011/launcher-updates/latest.json`

### 4. 后端已验证通过

- `history` 已显示 `1.0.24`
- `check/windows/x86_64/1.0.23` 已返回 `1.0.24`
- `latest.json` 已返回：
  - updater 包地址
  - minisign 签名
  - setup.exe 下载地址

---

## 验证结果

- `1.0.24` 的 `.nsis.zip` 已生成
- `.sig` 已使用同一把 updater 私钥签出
- Python minisign 回验通过
- `3011` 发布成功
- `3011` 接口回读成功

---

## 备份

- pre: #136

---

## 待办清单

- 用户手动启动旧版客户端并验证：
  - 自动后台下载
  - 下载完成弹窗
  - 点击一次“立即更新”
  - 不再出现“下一步”交互
  - 安装完成后自动进入新版本

---

## 下一步建议

- 直接用旧版客户端手动验证 `1.0.23 -> 1.0.24`
- 若出现异常，优先回报具体失败点：
  - 检查不到更新
  - 下载不动
  - 点击后仍弹安装向导步骤
  - 安装完成后未自动拉起
