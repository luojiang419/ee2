# 进度快照 086 — 新版启动器深灰按钮透明度 50%

> 日期: 2026-05-25

---

## 已完成内容

### 1. 所有深灰按钮透明度统一调整为 50%

- 在 `EE2X魔改版启动器/src/styles.css` 中继续复用按钮主题变量
- 将以下变量的透明度统一调整为 `0.5`：
  - `--button-bg`
  - `--button-bg-hover`
  - `--button-bg-secondary`
- 因为各类按钮都依赖这三组变量，所以以下按钮同步生效：
  - `mini-action`
  - `mini-secondary`
  - `primary-glow`
  - `stack-action`
  - `text-button`
  - `path-button`
  - `picker-button`
  - `start-button`
  - `user-pill`

### 2. 背景板透明度维持上一版 20%

- 本次未调整 `--panel-bg`
- 当前背景板仍然保持 `rgba(9, 14, 21, 0.2)`

### 3. 已完成构建与安装包验证

- 在 `EE2X魔改版启动器/` 执行 `npm run build`
- 在 `EE2X魔改版启动器/` 执行 `npm exec tauri build`
- 已重新生成 `1.0.1` NSIS 安装包用于测试

---

## 当前修改模块

- `EE2X魔改版启动器/src/styles.css`

---

## 待办清单

- 实机查看按钮在复杂背景图上的可见度是否仍足够
- 观察悬停态在 50% 透明下是否还需要进一步提亮或加强边框
- 如果按钮显得过淡，下一轮优先单独提高 hover 态透明度而保留默认态 50%

---

## 下一步建议

- 直接安装测试包检查首页、设置页、更新页按钮
- 如果需要，我下一轮可以把“普通态 50% / hover 态 65%”拆开做
