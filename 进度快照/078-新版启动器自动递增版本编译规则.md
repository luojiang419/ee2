# 进度快照 078 — 新版启动器自动递增版本编译规则

> 日期: 2026-05-25

---

## 已完成内容

### 1. 新版启动器新增自动版本递增脚本

- 新增文件：`EE2X魔改版启动器/scripts/bump-version.mjs`
- 功能：
  - 读取当前版本号
  - 自动执行 `+0.0.1`
  - 同步更新以下版本源：
    - `package.json`
    - `package-lock.json`
    - `src-tauri/Cargo.toml`
    - `src-tauri/tauri.conf.json`

### 2. 版本基线已统一为 `1.0.0`

- 已将新版启动器版本起点统一为 `1.0.0`
- 本次实际编译验证已从：
  - `1.0.0 -> 1.0.1`

### 3. 正式编译入口已收敛

- 修改文件：
  - `EE2X魔改版启动器/package.json`
  - `EE2X魔改版启动器/src-tauri/tauri.conf.json`
- 新脚本规则：
  - `build:frontend`：只做前端构建
  - `build`：转发到 `build:frontend`
  - `build:release`：先自动递增版本，再执行 `tauri build`
- `beforeBuildCommand` 已改为：
  - `npm run build:frontend`
- 结果：
  - 正式编译只递增一次
  - 安装包文件名与新版本号一致

### 4. 安装包文件名已验证随版本自动递增

- 本次编译命令：
  - `npm run build:release`
- 实际输出安装包：
  - `EE2X魔改版启动器_1.0.1_x64-setup.exe`
- 说明：
  - 旧的 `npm exec tauri build` 直调方式已不再作为正式编译入口
  - 正式编译必须走 `build:release`

### 5. 规则文档已更新

- 修改文件：`地球帝国游戏开发规则.md`
- 已新增：
  - `10.7 新版启动器编译规则（EE2X魔改版启动器）`
- 已写明：
  - 版本从 `1.0.0` 起步
  - 每次正式编译自动 `+0.0.1`
  - 正式编译命令必须使用 `npm run build:release`
  - 安装包文件名自动携带新版本号

### 6. 构建验证

- `npm run build:release`：通过
- 生成产物：
  - `EE2X魔改版启动器/src-tauri/target/release/ee2x_mod_launcher.exe`
  - `EE2X魔改版启动器/src-tauri/target/release/bundle/nsis/EE2X魔改版启动器_1.0.1_x64-setup.exe`

---

## 当前修改到的模块

- `EE2X魔改版启动器/scripts/bump-version.mjs`
- `EE2X魔改版启动器/package.json`
- `EE2X魔改版启动器/package-lock.json`
- `EE2X魔改版启动器/src-tauri/Cargo.toml`
- `EE2X魔改版启动器/src-tauri/tauri.conf.json`
- `地球帝国游戏开发规则.md`

---

## 待办清单

- [ ] 后续每次正式编译统一使用 `npm run build:release`
- [ ] 如需要，可清理 `target/release/bundle/nsis/` 中旧版本安装包，避免目录堆积

---

## 下一步

- 提交并推送本轮自动递增版本编译规则
