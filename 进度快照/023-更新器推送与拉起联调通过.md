# 进度快照 023 — 更新器推送与拉起联调通过

> 日期: 2026-05-20

---

## 已完成内容

### 更新器新增无界面测试模式

已为新更新器增加 `--headless` 模式：

- 入口：`ee2x_update_suite.updater_gui`
- 无需 `tkinter`
- 可在当前环境直接拉起更新器进程执行更新
- 更新结果以 JSON 输出

### 新增端到端烟雾测试脚本

已新增：

- `更新器/src/ee2x_update_suite/publisher/smoke_test.py`

能力：

1. 构造临时 `Empire Earth II/` 测试根目录
2. 生成标准更新包
3. 推送到指定测试频道
4. 拉起更新器 `--headless`
5. 验证目标文件被更新
6. 验证启动器重启参数为 `--updated`

### 真实联调结果

已使用频道：

- `codex-e2e-3`

执行命令：

```bash
PYTHONPATH='更新器/src' python3 -m ee2x_update_suite.publisher.smoke_test --config '更新器/config/publish.local.json' --channel 'codex-e2e-3'
```

结果：

- 发布成功
- 远端 `latest.json`、manifest、ZIP 均可访问
- 更新器真实拉起成功
- `updatedFiles = 1`
- `rolledBack = false`
- `restartedLauncher = true`
- 伪启动器收到参数：`--updated`

---

## 修改的文件

| 文件 | 修改内容 |
|:-----|:---------|
| `更新器/src/ee2x_update_suite/updater_gui/runner.py` | 抽离无界面更新执行逻辑 |
| `更新器/src/ee2x_update_suite/updater_gui/__main__.py` | 增加 `--headless` 模式 |
| `更新器/src/ee2x_update_suite/publisher/smoke_test.py` | 新增端到端烟雾测试脚本 |
| `更新器/README.md` | 增加烟雾测试命令 |
| `更新器/docs/服务器部署说明.md` | 补充 `codex-e2e-3` 联调结果 |

---

## 已验证

- `python3 -m compileall 更新器/src`
- `codex-e2e-3` 频道发布成功
- 更新器 headless 真实执行成功
- 启动器桩程序重启成功

---

## 待办 / 遗留问题

- [ ] 在 Windows 环境打包真实 `ee2x-up.exe`
- [ ] 把真实 `ee2x-up.exe` 放入启动器 `update/`
- [ ] 选定正式发布文件范围
- [ ] 发布第一个正式 `stable` 版本
- [ ] 用真实游戏目录验证正式一键更新

---

## 下一步

- 准备正式版本清单与版本号
- 生成正式发布包
- 推送到 `stable`
- 做真实玩家环境验证
