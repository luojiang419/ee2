# 进度快照 054 — 服务器版本详情改为右侧抽屉

> 日期: 2026-05-21

---

## 已完成内容

### 1. 已将版本详情从模态弹窗改为右侧抽屉

- `更新器/flutter_publish_tool/lib/main.dart`
- 原先行为：
  - 点击历史版本卡片后尝试弹出模态对话框
  - 在小窗口场景下容易出现“看不到详情页”的问题
- 当前行为：
  - 点击卡片后在当前页面右侧滑出详情抽屉
  - 抽屉显示：
    - 版本号
    - Release ID
    - 推送时间
    - 最后下载时间
    - 启动器/游戏包大小
    - 下载次数
    - 资源变更 / 删除变更
    - 完整更新内容

### 2. 已补齐抽屉关闭与切换逻辑

- 点击左侧遮罩可关闭
- 点击右上角关闭按钮可关闭
- 点击底部 `关闭` 按钮可关闭
- 切换离开 `服务器版本` 标签页时，详情抽屉会自动关闭

### 3. 历史弹窗里的“查看详情”已接到同一逻辑

- 如果从“版本历史”弹窗里点 `查看详情`
- 会先关闭当前弹窗
- 再打开主页面右侧详情抽屉
- 避免出现一个入口能打开、另一个入口无反应的分叉行为

### 4. 本次验证

- 已通过：
  - `flutter analyze`
  - `build_flutter_publish_tool.bat`
- 本次构建标记：
  - `BUILD_ID=20260521-155320`
  - `BUILD_TIME=2026-05-21`
- 正式产物目录已更新：
  - `更新器/dist/ee2x-flutter-publisher/`

---

## 当前修改到的模块

- `更新器/flutter_publish_tool/lib/main.dart`
- `更新器/dist/ee2x-flutter-publisher/`（已重编正式产物）

---

## 待办清单

- [ ] 重新打开正式 EXE，点击 `服务器版本` 历史卡片，确认右侧抽屉已实际滑出
- [ ] 如有需要，再微调抽屉宽度、遮罩深浅、信息卡片密度
- [ ] 提交并推送本次右侧抽屉修复

---

## 下一步

- 直接使用新构建的 `ee2x_publish_tool.exe`
- 重点确认：
  1. 点击历史卡片后是否立即出现右侧详情抽屉
  2. 小窗口下抽屉内容是否可完整滚动查看
  3. 关闭按钮、遮罩点击、标签切换三种关闭方式是否都正常
