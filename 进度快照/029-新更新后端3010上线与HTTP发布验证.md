# 进度快照 029 — 新更新后端3010上线与HTTP发布验证

> 日期: 2026-05-20

---

## 已完成内容

### 1. 新更新后端已本地实现

- 新源码目录：
  - `更新器/update_backend_mg/`
- 已包含：
  - FastAPI 入口
  - SQLite schema
  - 历史导入逻辑
  - HTTP 发布接口
  - 静态 `/updates` 挂载
  - systemd 服务模板
  - 自动部署脚本

### 2. 新更新后端已真实部署到服务器

- 远端目录：
  - `/root/ee2x/ee2x_up-mg`
- 服务名：
  - `ee2x-update-mg.service`
- 端口：
  - `3010`
- 当前状态：
  - `active`
- 对外健康检查：
  - `http://115.231.35.105:3010/api/update/v1/health`

### 3. 已从旧 1234 更新目录导入历史

- 数据源：
  - `/opt/ee2x_up-xg/updates`
- 导入目标：
  - `/root/ee2x/ee2x_up-mg/storage/updates`
- 导入后已重建 SQLite 索引
- 已将导入频道的 `latest.json` 链接重写为 `3010` 地址

### 4. bridge 已切到新 HTTP 发布链路

- 新命令：
  - `prepare-dual`
  - `publish-http`
  - `release-history`
- `publish-dual` 仍保留为兼容入口，但底层推荐使用：
  - 本地 `prepare-dual`
  - 后端 `publish-http`

### 5. Flutter 发布端已改连新后端

- `remote-info` 已读 `3010`
- 版本历史按钮已直连：
  - `/api/update/v1/channels/{channel}/history`
- 主界面推送流程已切为：
  - 本地 `prepare-dual`
  - 再 `publish-http`

### 6. 启动器源码与本地配置已改到 3010

- 启动器本地配置文件：
  - `Config/ee2x-launcher.json`
  - `Config/ee2x-settings.json`
- 启动器源码默认值：
  - `main.js`
  - `renderer.js`
- 版本检查逻辑已优先读取：
  - `GET /api/update/v1/channels/{channel}/latest`

### 7. 已完成真实 HTTP 发布测试

- 测试频道：
  - `codex-http-test-1779275870`
- 发布方式：
  - 新后端 `3010`
  - `prepare-dual + publish-http`
- 验证通过：
  - `latest` 返回 200
  - `history` 返回 200
  - 双包 URL 指向 `3010`

### 8. 已完成服务端历史读取测试

- `release-history` CLI 正常
- 兼容接口 `/api/version/history` 也可返回新后端数据

### 9. Flutter Windows 产物已重建

- `flutter analyze` 通过
- 主构建目录若被旧 EXE 占用，已手动回写到备用目录：
  - `更新器/dist/ee2x-flutter-publisher-next/`

---

## 当前修改到的模块

- `更新器/update_backend_mg/`
- `更新器/src/ee2x_update_suite/publisher/http_backend.py`
- `更新器/src/ee2x_update_suite/bridge/__main__.py`
- `更新器/config/publish.example.json`
- `更新器/config/publish.local.json`
- `更新器/flutter_publish_tool/lib/main.dart`
- `更新器/README.md`
- `更新器/docs/服务器部署说明.md`
- `更新器/docs/发布协议.md`
- `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/main.js`
- `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/renderer/renderer.js`
- `Empire Earth II/地球帝国二代远航版启动器/Config/ee2x-launcher.json`
- `Empire Earth II/地球帝国二代远航版启动器/Config/ee2x-settings.json`

---

## 待办清单

- [ ] 决定 `stable` 首个正式版本要发布什么内容，并通过新后端发布
- [ ] 用真实启动器跑一次完整：
  - `release:status`
  - launcher 自升级
  - game 强更
- [ ] 清理或下线旧 `1234` 更新职责前，再检查是否还有残余引用
- [ ] 视需要把 `dist/ee2x-flutter-publisher-next/` 回写成主产物目录

---

## 下一步

- 直接用新 Flutter 发布端或 CLI 向 `stable` 发第一版正式双包
- 发版后用启动器连 `3010` 做完整更新验收
