# 进度快照 032 — 更新推送链路打通与版本删除完成

> 日期: 2026-05-20

---

## 已完成内容

### 1. 新更新后端已补齐版本删除能力

- 新增接口：
  - `DELETE /api/update/v1/channels/{channel}/releases/{releaseId}`
- 删除逻辑已落地：
  - 删除服务器存储中的对应 `releases/{releaseId}` 目录
  - 删除 SQLite 中的 release / package / file 记录
  - 删除当前 latest 时自动回退到上一版
  - 删除频道最后一版时，`latest` 变为不存在，频道回到“未发布”

### 2. 历史接口已增强

- `GET /api/update/v1/channels/{channel}/history`
- 现在返回：
  - `currentReleaseId`
  - `currentVersion`
  - `history[]`
- Flutter 发布端无需再猜当前生效版本

### 3. Python bridge 与 HTTP 客户端已补齐 delete-release

- 新增 bridge 命令：
  - `delete-release --config ... --release-id ...`
- Flutter 端删除版本统一复用本地 `publish.local.json` 和 `publishToken`

### 4. 端到端烟雾测试已升级并验证通过

- `更新器/src/ee2x_update_suite/publisher/smoke_test.py`
- 已升级为：
  - 双包 `prepare/publish-http`
  - 自动临时频道
  - launcher / game 两次 headless 接收更新
  - `deleteList` 实际删除验证
  - 删除普通历史版本验证
  - 删除当前 latest 自动回退验证
  - 删除最后版本后 `latest=404` 验证

### 5. 已完成本地临时后端烟雾测试

- 本地 `127.0.0.1:3011` 临时后端已成功跑通完整链路
- 验证结果：
  - 3 次发布成功
  - 2 次 headless 接收成功
  - 3 种删除场景成功

### 6. 已完成真实 3010 服务热更新与实测

- 远端部署目录：
  - `/root/ee2x/ee2x_up-mg/app/`
- 已热更新文件：
  - `main.py`
  - `service.py`
- 热更新前已自动生成时间戳备份
- 服务已重启并保持：
  - `ee2x-update-mg.service = active`
- 真实 `3010` 烟雾测试已通过：
  - 临时频道 `codex-http-smoke-1779279050`
  - 发布 / 接收 / 删除 / 回退 全部成功

### 7. Flutter 发布端已完成交互修复

- 主按钮仍走真实链路：
  - `prepare-dual -> publish-http`
- 已新增持久状态提示区：
  - 发布配置缺失
  - 未勾选文件
  - 版本号为空
  - 推送失败
  - 推送成功
- 版本历史弹窗已支持：
  - 当前版本标记
  - 删除版本
  - 二次确认
  - 删除中禁用重复点击
  - 删除后原地刷新历史和远端状态

### 8. Windows 主目录版发布端已重新编译

- 构建脚本已调整：
  - 主目录版更新失败时明确报错
  - `ee2x-flutter-publisher-next` 仅作回退产物，不再默认为最新
- 新主产物：
  - `更新器/dist/ee2x-flutter-publisher/ee2x_publish_tool.exe`
- 时间戳已更新为：
  - `2026-05-20 20:15:36`
- 已验证可启动，不是旧 EXE

---

## 当前修改到的模块

- `更新器/update_backend_mg/app/main.py`
- `更新器/update_backend_mg/app/service.py`
- `更新器/src/ee2x_update_suite/bridge/__main__.py`
- `更新器/src/ee2x_update_suite/publisher/http_backend.py`
- `更新器/src/ee2x_update_suite/publisher/smoke_test.py`
- `更新器/flutter_publish_tool/lib/main.dart`
- `更新器/build_flutter_publish_tool.bat`
- `更新器/README.md`
- `更新器/update_backend_mg/README.md`
- `更新器/docs/服务器部署说明.md`

---

## 待办清单

- [ ] 在 Windows 上人工打开新版 `ee2x_publish_tool.exe`，手点一次：
  - 勾选文件
  - 填版本号和更新内容
  - 推送到测试频道或正式频道
- [ ] 视需要把“删除版本”扩展为主界面级别的独立管理入口
- [ ] 如后续要纳入版本控制，需确认 `更新器/` 的独立 Git 管理策略（当前被根仓库 `.gitignore` 排除）

---

## 下一步

- 直接运行：
  - `更新器/dist/ee2x-flutter-publisher/ee2x_publish_tool.exe`
- 优先手测两项：
  - “推送更新到服务器” 是否有清晰成功/失败反馈
  - “版本历史” 中删除版本后，服务器文件和记录是否同步消失
