# 进度快照 051 — Win侧重编正式EXE并完成3010保守部署

> 日期: 2026-05-21

---

## 已完成内容

### 1. Win 侧正式构建链路已补齐 Flutter 路径容错

- `更新器/build_flutter_publish_tool.bat`
- 现在的 Flutter 查找顺序为：
  1. `FLUTTER_BIN` 环境变量
  2. `G:\data\flutter\bin\flutter.bat`
  3. `D:\flutter\bin\flutter.bat`
- 这样当前机器无需手工改脚本即可直接重编正式发布端

### 2. Win 侧正式产物已重新构建

- 已完成：
  - `flutter analyze`
  - `build_flutter_publish_tool.bat`
- 产物目录：
  - `更新器/dist/ee2x-flutter-publisher/`
- 关键文件已确认存在：
  - `ee2x_publish_tool.exe`
  - `ee2x-bridge.exe`
  - `BUILD_INFO.txt`
- 本次构建标记：
  - `BUILD_ID=20260521-151430`
  - `BUILD_TIME=2026-05-21`

### 3. 3010 部署脚本已改为“先备份、后替换、保留现有数据”

- `更新器/update_backend_mg/deploy_backend.py`
- 已移除旧逻辑：
  - 直接删远端目录
  - 无条件执行 `import_legacy`
- 新逻辑：
  - 先备份远端：
    - `db/`
    - `storage/`
    - `.env`
    - `ee2x-update-mg.service`
  - 再替换远端代码目录
  - 恢复现有 `db/storage/.env`
  - 仅在首次部署且远端无现有数据时才执行 `import_legacy`

### 4. 已实际部署到 3010 远端环境

- 部署脚本已真实执行成功
- 部署输出：
  - `BACKUP_DIR=/root/ee2x/backups/ee2x-update-mg-20260521-071837`
  - `IMPORTED_LEGACY=0`
- 说明：
  - 本次确实没有回退去重导旧 `1234` 数据
  - 3010 当前线上数据已保留

### 5. 3010 接口与下载计数验证已完成

- `GET /api/update/v1/health` 返回 `200`
- `GET /api/update/v1/channels/stable/history?limit=0` 返回 `200`
- `GET /updates/stable/latest.json` 返回 `200`
- `GET /publish/` 返回 `200`
- stable 当前状态：
  - `currentReleaseId=1.0.0`
  - `currentVersion=1.0.0`
  - `historyTotalCount=1`
- 下载计数验证：
  - 下载前 `downloadCount=0`
  - 实际请求当前 `game` 包一次后
  - 下载后 `downloadCount=1`
  - `downloadCountDelta=1`

### 6. 备份前后数据保全验证已完成

- 通过远端对比备份库与当前库确认：
  - stable 发布数一致：`1 -> 1`
  - `stableCurrentReleaseId` 一致：`1.0.0 -> 1.0.0`
  - `stableCurrentVersion` 一致：`1.0.0 -> 1.0.0`
  - `stableUpdatedAt` 一致，未发生意外回退

---

## 当前修改到的模块

- `更新器/build_flutter_publish_tool.bat`
- `更新器/update_backend_mg/deploy_backend.py`
- `更新器/README.md`
- `更新器/docs/服务器部署说明.md`
- `更新器/update_backend_mg/README.md`
- `更新器/dist/ee2x-flutter-publisher/`（已实际重编）

---

## 待办清单

- [ ] 如需肉眼最终确认，手工打开 `ee2x_publish_tool.exe` 检查“服务器版本”标签页弹窗样式
- [ ] 处理当前机器 GitHub 凭据问题并推送最新提交
- [ ] 如后续继续发布 stable 新版本，可直接基于当前 3010 环境继续使用

---

## 下一步

- 直接从当前已部署的 3010 后端继续做真实版本发布测试
- 如果后续还要增强历史页，可围绕：
  1. 详情弹窗排版微调
  2. 更多版本筛选/搜索
  3. 下载量展示细分
  继续迭代
