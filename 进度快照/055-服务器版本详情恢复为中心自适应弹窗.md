# 进度快照 055 — 服务器版本详情恢复为中心自适应弹窗

> 日期: 2026-05-21

---

## 已完成内容

### 1. 已取消右侧抽屉，恢复为页面中心弹窗

- `更新器/flutter_publish_tool/lib/main.dart`
- 你明确要求不要右侧抽屉栏
- 当前已将详情展示方式恢复为：
  - 软件页面屏幕中心弹窗
  - 不是侧边滑出面板

### 2. 已改为自适应弹窗大小 + 自适应字体大小

- 弹窗大小：
  - 根据当前窗口宽高动态计算
  - 不再使用固定 `860x620`
- 字体大小：
  - 基于当前可用窗口尺寸计算 `dialogScale`
  - 通过 `MediaQuery.textScaler` 统一缩放弹窗内部文字
- 目标：
  - 小窗口、小分辨率下也能完整显示
  - 大窗口下不至于显得过小

### 3. 弹窗内容区已保持可滚动

- 当窗口较小时：
  - 详情信息卡片区可滚动
  - 更新内容区可完整查看
- 保留关闭方式：
  - 点击右上角关闭按钮
  - 点击底部 `关闭`
  - 点击弹窗外遮罩关闭

### 4. 历史弹窗里的“查看详情”已保持兼容

- 如果从“版本历史”弹窗里点击 `查看详情`
- 会先关闭历史弹窗
- 再打开主页面中心详情弹窗

### 5. 本次验证

- 已通过：
  - `flutter analyze`
  - `build_flutter_publish_tool.bat`
- 本次构建标记：
  - `BUILD_ID=20260521-160112`
  - `BUILD_TIME=2026-05-21`
- 正式产物目录已更新：
  - `更新器/dist/ee2x-flutter-publisher/`

---

## 当前修改到的模块

- `更新器/flutter_publish_tool/lib/main.dart`
- `更新器/dist/ee2x-flutter-publisher/`（已重编正式产物）

---

## 待办清单

- [ ] 重开正式 EXE，点击 `服务器版本` 历史卡片，确认中心弹窗已出现
- [ ] 在不同窗口尺寸下检查字体缩放和内容区滚动是否符合预期
- [ ] 提交并推送本次中心自适应弹窗修复

---

## 下一步

- 直接打开新构建的 `ee2x_publish_tool.exe`
- 重点确认：
  1. 点击历史卡片后是否出现中心弹窗
  2. 窗口缩小时弹窗字体和尺寸是否自适应
  3. 更新内容较长时是否仍能完整滚动查看
