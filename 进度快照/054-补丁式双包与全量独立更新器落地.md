# 进度快照 054 — 补丁式双包与全量独立更新器落地

> 日期: 2026-05-21

---

## 已完成内容

### 1. 发布链已切换为“补丁模式”

- `更新器/update_backend_mg/app/service.py`
  - 服务端不再根据“上一版有、这次没选”自动推断 `deleteList`
  - 现在只原样接收并保存客户端 manifest 中显式提供的 `deleteList`
- `更新器/docs/发布协议.md`
  - 明确补丁模式语义：
    - 勾选什么就更新什么
    - 未勾选文件保持原状
    - 第一版默认禁用自动删除推断
- `更新器/README.md`
  - 已同步更新说明

### 2. 打包工具摘要与 Web 推送页文案已同步

- `更新器/flutter_publish_tool/lib/main.dart`
  - 导出成功提示改为：
    - launcher 无补丁文件时不触发启动器自升级
    - 未勾选文件不会被自动删除
    - 若未来存在删除项，视为“显式删除项”
- `更新器/update_backend_mg/app/publish/assets/app.js`
  - Web 推送页结果与历史标签中的“自动删除”改为“显式删除”
  - 成功摘要新增“未勾选文件不会被自动删除”

### 3. 独立更新器已支持 `scope=all`

- `更新器/src/ee2x_update_suite/shared/constants.py`
- `更新器/src/ee2x_update_suite/shared/models.py`
- `更新器/src/ee2x_update_suite/shared/updater_core.py`
- `更新器/src/ee2x_update_suite/updater_gui/__main__.py`
- `更新器/src/ee2x_update_suite/updater_gui/runner.py`
- `更新器/src/ee2x_update_suite/updater_gui/app.py`

新行为：

- CLI 新增 `--scope all`
- `all` 模式会：
  - 读取一次 `latest.json`
  - 自动判断 `launcher` / `game` 是否需要更新
  - 按 `launcher -> game` 顺序执行
  - 全部完成后只重启一次启动器
- headless / GUI 结果现在会输出：
  - `executedScopes`
  - `skippedScopes`
  - `scopeSummaries`

### 4. 启动器运行时代码已按“全量独立更新器”方向热修

本机源码热修：

- `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/main.js`
- `Empire Earth II/地球帝国二代远航版启动器/Core/resources/app/renderer/renderer.js`

实际安装版热修：

- `D:\下载\Empire Earth II 远航版8.49.zip\Empire Earth II 远航版8.49\Empire Earth II\地球帝国二代远航版启动器\Core\resources\app\main.js`

行为变化：

- 默认按 `all` 模式拉起独立更新器
- 若当前线上 `launcher` 包仍是 delete-only 坏包，则自动保守降级为 `game`
- 更新器启动成功后，启动器会主动退出，让出文件占用与前台焦点

### 5. 端到端验证已完成

- `python3 -m compileall 更新器/src/ee2x_update_suite 更新器/update_backend_mg/app` 通过
- 本地补丁规则验证通过：
  - 只勾 `zips_ee2x/EE2X_db.zip` 时：
    - `launcher.files = 0`
    - `launcher.deleteList = []`
    - `launcherTriggersSelfUpdate = false`
- `publisher.smoke_test` 已改为优先拉起本地后端进行验证
- 烟雾测试结果通过：
  - 三次发布成功
  - `all` 模式按 `launcher -> game` 执行
  - 未勾选的 `obsolete` 文件在 v2 中保持存在，不会被自动删除
  - history / latest 回退与 bundle 发布链继续正常

---

## 当前修改到的模块

- `更新器/update_backend_mg/app/service.py`
- `更新器/update_backend_mg/app/publish/assets/app.js`
- `更新器/src/ee2x_update_suite/shared/constants.py`
- `更新器/src/ee2x_update_suite/shared/models.py`
- `更新器/src/ee2x_update_suite/shared/updater_core.py`
- `更新器/src/ee2x_update_suite/updater_gui/`
- `更新器/src/ee2x_update_suite/publisher/smoke_test.py`
- `更新器/flutter_publish_tool/lib/main.dart`
- `更新器/docs/发布协议.md`
- `更新器/README.md`
- `Empire Earth II/.../Core/resources/app/*.js`（本机热修，目录被 `.gitignore` 排除）
- 实际安装目录 `D:\下载\...\地球帝国二代远航版启动器\Core\resources\app\main.js`（本机热修）

---

## 待办清单

- [ ] 将新的 `update_backend_mg` 部署到 3010
- [ ] 部署后执行一次：
  - `POST /api/update/v1/channels/stable/latest/rebuild`
  修正当前线上 `stable/latest.json`
- [ ] 重建正式 launcher 包，将本机热修转化为正式可分发版本
- [ ] 在 Windows 真机上用真实启动器点一次更新，确认：
  - 独立更新器窗口可见
  - 原启动器退出
  - 实际文件更新成功

---

## 下一步

- 先部署后端与 rebuild latest
- 再重建并发布新的 launcher 修正版
- 最后用当前安装版与新 launcher 包分别做一次真机验收
