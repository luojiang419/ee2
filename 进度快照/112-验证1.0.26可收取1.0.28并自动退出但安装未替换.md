# 进度快照 112 — 验证 1.0.26 可收取 1.0.28 并自动退出但安装未替换

> 日期: 2026-05-27

---

## 已完成内容

### 1. 服务器 latest 已推进到 1.0.28

- 当前 `3011` 最新版本：
  - `1.0.28`
- 当前接口验证通过：
  - `history`
  - `check/windows/x86_64/1.0.26`
  - `latest.json`

### 2. 已完成真实隔离安装验证环境

- 隔离安装目录：
  - `G:\ee2x-validate-126\install`
- 隔离配置目录：
  - `G:\ee2x-validate-126\appdata`
- 该隔离安装中的启动器版本已确认是：
  - `1.0.26`

### 3. 已验证通过的链路

在隔离 `1.0.26` 环境中，已经实测确认：

- 客户端会主动请求：
  - `GET /api/launcher-update/v1/check/windows/x86_64/1.0.26`
- 服务端会返回：
  - `200 OK`
- 客户端会自动下载：
  - `GET /launcher-updates/releases/1.0.28/updater.nsis.zip`
- `updaterDownloadCount` 已增长到 `9`

说明：

- **启动器新版本推送可收到**
- **updater 包可自动下载**

### 4. 当前未闭环点

在同一轮隔离验证中还确认到：

- `1.0.26` 旧进程会自动退出
- 但安装目录中的：
  - `G:\ee2x-validate-126\install\ee2x_mod_launcher.exe`
- 文件版本仍保持：
  - `1.0.26`

因此当前最新结论是：

- **检查链路通过**
- **下载链路通过**
- **旧进程退出通过**
- **安装替换落盘仍未验证成功**

---

## 机器当前安装状态

- 系统正式安装：
  - `C:\Program Files\EE2X魔改版启动器\`
  - 版本：`1.0.23`
- 隔离验证安装：
  - `G:\ee2x-validate-126\install\`
  - 版本：`1.0.26`

说明：

- 当前机器上并没有另一份真实正式安装的 `1.0.26`
- 本轮所有 `1.0.26 -> 1.0.27/1.0.28` 验证，都是基于隔离安装目录完成

---

## 待办清单

- 查清为什么 `plugin-updater` 在当前场景下：
  - 能下载 updater 包
  - 能让旧进程退出
  - 但没有把安装目录替换成新版本
- 判断问题是否与以下任一项有关：
  - 自定义安装目录 `G:\ee2x-validate-126\install`
  - `currentUser` 安装模式
  - NSIS 静默参数未显式带安装目标目录
  - updater 包格式与 Tauri v2 运行时预期不完全一致

---

## 下一步建议

- 下一轮优先围绕“安装未替换落盘”继续排查
- 不再继续堆新版本号，先把 `1.0.28` 这条升级链彻底跑通
