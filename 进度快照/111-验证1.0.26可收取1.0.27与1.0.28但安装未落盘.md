# 进度快照 111 — 验证 1.0.26 可收取 1.0.27/1.0.28 但安装未落盘

> 日期: 2026-05-27

---

## 已完成内容

### 1. 版本已推进到 1.0.28

- 已构建并发布：
  - `1.0.27`
  - `1.0.28`
- 当前服务器 latest：
  - `1.0.28`

### 2. 1.0.26 客户端真实验证结果

已通过隔离安装目录 `G:\ee2x-validate-126\install` 做实测：

- `1.0.26` 客户端会主动请求：
  - `GET /api/launcher-update/v1/check/windows/x86_64/1.0.26`
- 服务端返回：
  - `200 OK`
- 客户端会自动下载：
  - `GET /launcher-updates/releases/1.0.27/updater.nsis.zip`
  - `GET /launcher-updates/releases/1.0.28/updater.nsis.zip`

这说明：

- **启动器新版本推送可以收到**
- **updater 包可以自动下载**

### 3. 安装阶段当前结论

在隔离安装目录验证时，出现以下现象：

- 旧版 `1.0.26` 进程会自动退出
- 但安装目录中的 `ee2x_mod_launcher.exe` 版本仍保持 `1.0.26`
- 未验证到自动替换落盘为 `1.0.27/1.0.28`

因此当前结论是：

- **检查 + 下载链路已验证通过**
- **安装落盘链路在这台机器的隔离安装场景下仍未闭环**

### 4. 系统真实安装状态

当前机器上识别到的安装记录：

- `1.0.23`：`C:\Program Files\EE2X魔改版启动器\`
- `1.0.26`：`G:\ee2x-validate-126\install\`（隔离验证安装）

未在系统默认路径中发现另一份真实 `1.0.26` 正式安装。

---

## 服务器侧验证结果

- `1.0.26 -> 1.0.27`：
  - `check` 命中
  - `updaterDownloadCount` 增加
- `1.0.26 -> 1.0.28`：
  - `check` 命中
  - `updaterDownloadCount` 增加

---

## 备份

- pre: #139
- pre: #140

---

## 待办清单

- 继续定位为什么下载后旧进程退出，但安装目录版本未切到新版本
- 判断问题是否只出现在自定义安装目录场景
- 若是，补“强制升级时显式带当前安装目录参数”的安装链

---

## 下一步建议

- 下一轮优先收口“安装器启动后未落盘”的根因
- 不再继续堆版本号，先修安装闭环，再发布下一版验证
