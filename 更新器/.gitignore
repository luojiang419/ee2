# 构建产物（exe/dll/打包输出）
dist/

# PyInstaller 构建中间产物
build/

# 发布包（含游戏文件、启动器、zip等）
releases/

# 旧版更新器源码（PyInstaller输出：exe/spec/toc/pyz等）
更新器源码/

# 临时文件
.flutter_publish_tool_tmp/
*.zip

# 本地配置（含密钥/密码，不推送）
config/publish.local.json

# 服务器敏感信息（VPS IP/密码等）
vps云服务器信息.txt
